#!/bin/sh
# Move to the Protractor test project folder
cd $HOME

echo "Running Protractor tests"
export GEOMETRY="1920x1080x24"
xvfb-run -n 10 --server-args="-screen 0 $GEOMETRY -ac +extension RANDR" protractor $CONF_FILE_PATH
echo "Protractor tests have Completed"

exit $RESULT
